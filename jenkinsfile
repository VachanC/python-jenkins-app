pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/VachanC/python-jenkins-app.git'
            }
        }


        stage('Run Python App') {
            steps {
                sh 'python app.py'
            }
        }
    }
}
